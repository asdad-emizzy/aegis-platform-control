Never bypass Platform API for business metadata.

Never store telemetry in PostgreSQL.

Never tightly couple to AWS SDK.

Always use provider abstraction.

Always support future multi-cloud.

Never duplicate Grafana functionality.